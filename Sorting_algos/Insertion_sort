def insertion_sort(a):
    for j in range(1,len(a)):
        key=a[j]
        i=j-1
        while i>-1 and a[i]>key:   #a[i]<key for decreasing order
            a[i+1]=a[i]
            i=i-1
        a[i+1]=key
    return a
def main():
    a=[2,5,4,6,1,3]
    print(insertion_sort(a))
main ()
    
